# Plans - 2026-01-07

- Rebuild the devcontainer to confirm the entrypoint installs and runs.
- Verify the stop hook message appears on container shutdown.
- Adjust entrypoint or stop logic if runtime behavior differs from expectations.
- Decide how to handle composed `.env` values with Docker `--env-file` (remove `--env-file` or generate resolved env file). (done: removed `--env-file`)
- Fix prompt color escapes to avoid literal `\[ \]` in init output. (done: raw ANSI escapes)

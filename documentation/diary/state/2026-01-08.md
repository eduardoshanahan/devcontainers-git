# State - 2026-01-08

- `.devcontainer/config/starship.toml` now mirrors the root prompt improvements: `PROJECT_NAME` env var, `line_break` module, `directory.format`, and spaced git status output.
- Reminder: set `PROJECT_NAME` in the environment to show the dynamic label.

# -----------------------------------------------------------------------------
# Devcontainer configuration (git layer)
#
# Copy to `.env` and edit values for your machine:
#   cp .env.example .env
# -----------------------------------------------------------------------------

# Project identity
PROJECT_NAME=git

# Host user mapping (must match your host user)
HOST_USERNAME=your_username
HOST_UID=1000
HOST_GID=1000

# Workspace folder inside the container (mounted from this repo)
WORKSPACE_FOLDER=/workspace

# Locale
LOCALE=en_IE.UTF-8

# Container resources
CONTAINER_MEMORY=4g
CONTAINER_CPUS=2
CONTAINER_SHM_SIZE=1g

# Git identity in the container
GIT_USER_NAME="Your Name"
GIT_USER_EMAIL=you@example.com

# -----------------------------------------------------------------------------
# Git sync (sync-git.sh)
# -----------------------------------------------------------------------------

# Optional default remote URL used when adding the primary remote (supports https:// or git@).
# GIT_REMOTE_URL=git@github.com:org/repo.git

# Required for sync-git.sh: list of remotes to sync (space or comma separated).
# Example: "origin lan"
# GIT_SYNC_REMOTES="origin"

# Optional: list of remotes to push to after syncing (space or comma separated).
# GIT_SYNC_PUSH_REMOTES="origin"

# Optional: per-remote URLs used when a remote name in GIT_SYNC_REMOTES is missing.
# GIT_REMOTE_URL_ORIGIN=git@github.com:org/repo.git
# GIT_REMOTE_URL_LAN=ssh://git@192.168.1.10:/volume1/git/${PROJECT_NAME}.git

# Editor choice for editor-launch.sh
EDITOR_CHOICE=code

# Image build tag used by launchers
DOCKER_IMAGE_TAG=latest

# Dedicated workflow image/container names (launchers set sensible defaults; override if needed)
DOCKER_IMAGE_NAME="${PROJECT_NAME}-editor"
CONTAINER_HOSTNAME_EDITOR="${PROJECT_NAME}-editor"
CONTAINER_HOSTNAME_DEVCONTAINER="${PROJECT_NAME}-devcontainer"
CONTAINER_HOSTNAME_CLAUDE="${PROJECT_NAME}-claude"

# The devcontainer uses CONTAINER_HOSTNAME for its hostname; launchers set this per-context.
CONTAINER_HOSTNAME="${CONTAINER_HOSTNAME_EDITOR}"

# -----------------------------------------------------------------------------
# Maintenance
# -----------------------------------------------------------------------------

# Required by clean-devcontainer-images.sh: prune unused devcontainer images older than N days.
DEVCONTAINER_IMAGE_RETENTION_DAYS=7

# -----------------------------------------------------------------------------
# Claude Code (build-time opt-in)
# -----------------------------------------------------------------------------

# Install Claude CLI during devcontainer image build.
# - The Claude launcher forces this on.
INSTALL_CLAUDE=false

# Optional: verify the downloaded Claude installer with a SHA256 checksum (64 hex chars).
# Leave empty to skip verification.
CLAUDE_INSTALL_SHA256=
